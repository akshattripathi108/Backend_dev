const user = require('./user_response');


// The callback module is imported and executed automatically when this file runs.const fileHandling = require('./file_handling');

// The file_handling module is imported and executed automatically when this file runs.const task = require('./task');

// The task module is imported but not executed automatically. Its functions can be used as needed.